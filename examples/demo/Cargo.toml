[package]
name = "textus-demo"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
textus = { path = "../../crates/textus" }

[package.metadata.textus.i18n]
languages = ["en", "ko"]
