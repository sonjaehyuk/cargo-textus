[package]
name = "textus-demo"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
cargo-textus = { path = "../../crates/cargo-textus", default-features = false }

[package.metadata.textus.i18n]
languages = ["en", "ko"]
