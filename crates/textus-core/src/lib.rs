//! 문서 언어 검증과 파일 선택 규칙을 공유하는 코어 라이브러리.
//!
//! CLI 실행, 매크로 토큰, 브라우저 및 파일 읽기에 의존하지 않는 규칙을 제공한다.
//! CLI와 절차적 매크로가 동일한 언어 검증을 사용하도록 별도 크레이트로 분리한다.

/// ISO 639-1 코드 검증과 두 문서 경로 선택 방식을 제공한다.
///
/// 파일 시스템에 접근하지 않고 선택할 경로만 반환하므로 호출자가 파일 읽기와
/// 오류 표시를 담당하며 언어 선택 규칙은 독립적으로 테스트할 수 있다.
pub mod i18n;
