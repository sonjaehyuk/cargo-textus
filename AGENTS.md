# cargo-textus 작업 지침

## 프로젝트 목적과 범위

- Rust 프로젝트의 문서화를 풍부하게 만드는 Cargo 확장 도구를 개발한다.
- 첫 번째 기능은 i18n이다. 사용자가 직접 작성하거나 지정한 언어별 문서를 선택하고 열 수 있도록 한다.
- 자동 번역 도구가 아니다. 번역 생성이나 외부 번역 서비스 연동을 기본 기능으로 추가하지 않는다.
- 한국어를 요청하면 사용자가 지정한 한국어 문서를 제공하는 것이 핵심 사용 사례다.
- CLI는 `cargo textus i18n open --lang ko` 형태로 확정했다. `list`, `check`, `build --lang ko`, `build --lang ko --open`도 같은 명령 계층을 따른다.
- 사용자가 명시적으로 사용하는 textus 전용 매크로로 문서 파일을 선택한다. 기본 `include_str!`를 덮어쓰거나 `doc` 속성의 의미를 바꾸거나 사용자 소스를 치환하지 않는다.
- 그 외 세부 문법과 설정은 초안 구현의 결정과 사용자 확정 요구사항을 구분한다.
- 앞으로 i18n 이외의 기능이 추가될 수 있음을 고려한다.
- `cargo-textus` 패키지가 CLI 바이너리와 절차적 매크로 라이브러리를 함께 제공한다. 공개 매크로 경로는 `cargo_textus::`이며 별도 `textus` 패키지는 배포하지 않는다.
- CLI 전용 의존성은 기본 활성화되는 `cli` feature에 둔다. 매크로만 사용하는 프로젝트는 `default-features = false`로 사용할 수 있어야 한다.

## 문서 선택 방식: 요구사항과 현재 구현

- 사용자 요구사항: 기존 `cargo_textus::include_str!`의 파일명 방식(`greet.md` → `greet.ko.md`)을 기본으로 유지한다. 디렉토리 방식은 기존 매크로를 변경하지 않고 새 매크로로 제공한다.
- 이번 구현에서 채택한 문법은 `cargo_textus::include_str_from_dir!("docs/greet.md", ko = "docs/ko/", en = "translations/english")`이다. 언어 코드와 실제 디렉토리 이름을 분리하며 구체적인 매크로 이름·문법은 구현의 설계 결정으로 기록한다.
- 두 매크로 모두 경로는 호출 패키지의 `Cargo.toml` 기준이다. 디렉토리 방식은 기본 경로의 파일명 전체만 매핑 디렉토리에 붙이며 상위 디렉토리를 복사하거나 언어 접미사를 추가하지 않는다.
- 디렉토리 매핑은 하나 이상 필요하다. 선택하지 않은 매핑도 ISO 639-1 코드, 중복 키, 경로를 검증한다. 디렉토리 끝의 `/` 하나는 허용하며 파일 존재 여부는 선택한 파일만 검사한다.
- `[package.metadata.textus.i18n].languages`는 CLI의 지원 언어 선언이고, 매크로의 경로 매핑은 각 호출의 문서 선택 설정이다. 둘을 자동 추론하거나 같은 설정으로 취급하지 않는다.
- 공통 언어 검증과 경로 선택은 `textus-core`가 담당한다. 파일 읽기와 변경 추적은 확장 결과의 Rust 기본 `include_str!`에 맡긴다.
- `TEXTUS_LANG`는 CLI가 자식 빌드에 전달하는 내부 규약이다. 매크로 크레이트 컴파일 시점의 `option_env!`를 사용해 Cargo의 환경변수 변경 추적을 유지한다. 확장 시점의 추적되지 않는 환경변수 읽기로 바꾸지 않는다.
- 현재 CLI는 별도 안내 파일을 여는 대신 rustdoc API 문서를 생성한다. `check`도 실제 문서 빌드이며 doctest나 번역 완전성 검사는 아니다. 삭제된 포함 파일의 재검사를 위해 언어·패키지별 전용 경로의 생성 문서를 `cargo clean --doc`으로 정리한 뒤 빌드한다.

## 브랜치와 커밋

- 모든 작업과 커밋은 현재의 `i18n` 브랜치 안에서만 수행한다. 새 브랜치나 다른 브랜치의 worktree를 만들거나 다른 브랜치로 전환하지 않는다.
- 작업 시작 전과 커밋 전에 현재 브랜치 및 작업 트리 상태를 확인한다. 브랜치가 `i18n`이 아니면 파일을 변경하지 말고 사용자에게 상황을 알린다.
- 의미 있는 작업 단위가 완료될 때마다 필요한 검증을 마친 뒤 커밋한다. 문서 작업도 독립적인 작업 단위로 커밋한다.
- 커밋은 하나의 목적을 담도록 구성하고, 메시지에 변경 내용을 명확히 표현한다.
- 자신이 변경한 관련 파일만 명시적으로 스테이징한다. 기존 사용자 변경이나 관계없는 파일을 포함하거나 되돌리지 않는다.
- 별도 요청 없이 push하거나 기존 커밋 이력을 재작성하지 않는다.

## 코드 품질과 검증

- Rust 코드를 변경하면 `cargo fmt --all`로 포맷을 맞춘다.
- 커밋 전에 `cargo fmt --all -- --check`와 `cargo clippy --workspace --all-targets -- -D warnings`를 실행하고, 변경으로 발생한 문제를 해결한다.
- 동작 변경에는 관련 테스트를 추가하거나 수정하고, 변경 범위에 맞는 `cargo test`를 실행한다.
- 경고를 숨기기 위한 무분별한 lint 허용이나 검증 생략을 하지 않는다.
- 문서만 변경한 경우에는 내용과 diff를 검토한다. Rust 패키지가 아직 없는 경우처럼 명령을 적용할 수 없다면 실행 성공으로 보고하지 말고 미실행 사유를 기록한다.
- 최종 보고에는 변경 사항, 검증 결과 및 제한, 커밋 정보를 간결하게 포함한다.

## 언어 코드

- CLI, 설정, 문서 메타데이터에 사용하는 언어 식별자는 ISO 639-1을 따른다.
- 표준 표현은 소문자 두 글자 코드로 한다. 예: `ko`, `en`, `ja`.
- 두 글자라는 형식뿐 아니라 실제 ISO 639-1에 배정된 코드인지 검증한다. 검증용 데이터의 출처를 명확히 한다.
- `kor` 같은 세 글자 코드나 `ko-KR` 같은 지역 포함 태그를 ISO 639-1 코드로 취급하지 않는다.
- 유효한 언어 코드인지와 해당 프로젝트에 그 언어의 문서가 등록되어 있는지는 구분해서 검증하고 오류를 안내한다.
- 현재 구현은 선택한 언어의 파일이나 디렉토리 매핑이 없으면 오류를 내며 자동 fallback하지 않는다. 언어를 선택하지 않았을 때 기본 파일을 사용하는 것은 별도 동작이다. 향후 fallback을 추가하려면 별도로 설계하고 사용자가 모르게 다른 언어의 문서를 열지 않는다.

## 구조와 확장성

- `main.rs`는 실행 진입점, 인자 전달, 오류 출력 및 종료 코드 처리 등 최소 역할만 맡는다.
- CLI 정의와 명령 분배, 설정 로딩, 기능별 로직, 파일 및 브라우저 입출력은 책임에 따라 모듈로 분리한다.
- i18n 로직은 독립적인 기능 모듈에 둔다. 새 기능 추가가 i18n 내부 구현의 수정에 의존하지 않도록 한다.
- 공통 로직은 실제 재사용 필요가 생길 때 추출한다. 미래 기능을 위해 불필요한 플러그인 프레임워크를 미리 만들지 않는다.
- 언어 검증 및 문서 선택 로직은 CLI 실행이나 브라우저 실행 없이 테스트할 수 있도록 구성한다.
- 사용자 작성 원본과 생성 산출물을 구분하고, 문서 생성 과정에서 원본을 덮어쓰지 않는다.

## 문서화와 설계 결정

- test와 demo를 제외한 소스의 문서 주석은 한국어로 작성한다. 공개 API뿐 아니라 비공개 함수, struct, enum 변형, 필드, 상수, 모듈 등 작은 요소도 문서화한다.
- 단순한 이름 풀이를 넘어 요소의 의도, 사용 맥락, 호출 전제, 다른 계층과의 책임 경계, 오류 조건과 제한을 해당 요소에 필요한 깊이로 설명한다. 구현과 어긋나는 예시나 반복적인 형식 채우기는 피한다.

- 기능을 추가하거나 동작을 변경하면 사용 예시, 설정 방법, 오류 조건을 관련 문서에 함께 반영한다.
- 지원하는 문서 종류와 한계를 명시한다. 별도 안내 문서를 여는 기능과 rustdoc API 문서 자체를 언어별로 생성하는 기능을 구분한다.
- 아직 결정되지 않은 CLI나 파일 형식은 제안임을 표시하고, 구현하면서 채택한 결정과 이유를 기록한다.
